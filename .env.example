# ==========================================
# 🍽️ Meal Recommender API - Configuration
# ==========================================
# Copiez ce fichier en .env et modifiez les valeurs

# 🌍 Environnement
APP_ENV=development
DEBUG=false

# 🌐 Serveur API
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true

# 🛡️ Sécurité
# En production, limitez les origines CORS:
# CORS_ORIGINS=["https://monsite.com", "https://app.monsite.com"]
CORS_ORIGINS=["*"]

# Rate limiting: requêtes par minute par IP
API_RATE_LIMIT_PER_MINUTE=100

# 📊 Données
# data_source: "mealdb" (par défaut) ou "csv"
DATA_SOURCE=mealdb
# URL du dataset HuggingFace (ne pas modifier sauf si vous hébergez votre propre CSV)
CSV_URL=https://huggingface.co/spaces/BucKz96/csv_app/resolve/main/recipes_clean.csv
MEALDB_API_BASE=https://www.themealdb.com/api/json/v1/1

# Durée de vie du cache en secondes (1 heure = 3600)
CACHE_TTL_SECONDS=3600

# 📝 Logging
# Niveaux: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Format: json (production) ou text (développement)
LOG_FORMAT=text

# ==========================================
# Production (à décommenter pour Render/Docker)
# ==========================================
# APP_ENV=production
# DEBUG=false
# API_RELOAD=false
# LOG_FORMAT=json
# LOG_LEVEL=INFO
